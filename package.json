{
  "name": "faultlab",
  "private": true,
  "version": "0.2.1",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "pnpm --filter @faultlab/extension dev",
    "build": "pnpm --filter @faultlab/extension build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.11"
  }
}
